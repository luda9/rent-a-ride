<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
    <h3>Post your car for rent</h3>
      <%= simple_form_for [@car, @booking] do |f| %>
        <%= f.input :initial, as: :date %>
        <%= f.input :final, as: :date %>

        <%= f.submit class: 'btn btn-flat' %>
      <% end %>
    </div>
  </div>
</div>
